#!/usr/bin/env perl5.10.1

use strict;
use warnings;

use lib "$ENV{REFIMP_LIB}";
use lib "$ENV{REFIMP_PERL5LIB}";

use above "RefImp";
use RefImp::Command;
RefImp::Command->execute_with_shell_params_and_exit();
