#!/usr/bin/env lims-perl

use strict;
use warnings;

BEGIN {
    # FIXME Temporary
    if ( ! exists $ENV{REFIMP_CONFIG_FILE} ) {
        $ENV{REFIMP_CONFIG_FILE} = '/gscuser/ebelter/dev/refimp-env/config.prod.yml';
    }
};

use above "RefImp";
use RefImp::Command;
RefImp::Command->execute_with_shell_params_and_exit();
